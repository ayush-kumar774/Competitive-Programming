#include<bits/stdc++.h>
using namespace std;
int main()
{
  long long n, a, b, c ;
  cin >> n >> a >> b >> c ;
  string str[n];
  for(long long i = 0 ; i< n ; i++ ) cin >> str[i];
  if(a == 0 or b == 0 or c == 0) cout << "No\n";
  else{
    for(int i = 0 ; i < n ; i++)
    {
      if(s[i] == "AB")
      {
        if(a - 1 == 0)
        {
          if(b - 1 == 0)

        }
      }
    }
  }

}
