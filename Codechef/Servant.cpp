//https://www.codechef.com/problems/FLOW008
#include<bits/stdc++.h>
using namespace std ;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int testcase{0} ;
    cin >> testcase ;
    while(testcase--)
    {
        int num{0} ;
        cin >> num ;
        (num < 10) ? cout << "What an obedient servant you are!" : cout << "-1" ;
         cout << endl ;
    }
}
